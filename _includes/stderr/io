<article>
	<header>
		<hgroup class="post-header">		{% include stderr/setup %}
			<h2>{% if _LLIST_ %}<a href="{{ _LLIST_ }}">{{ _TITLE_ }}</a> <small>{{ _DESC_ }}{% if post %} <a href="{{ BASE_PATH }}{{ _URL_ }}" class="permalink muted"><i class="icon-link"></i></a>{% endif %}</small></h2>
			{% else %}{{ _TITLE_ }} <small>{{ _DESC_ }}{% if post %} <a href="{{ BASE_PATH }}{{ _URL_ }}" class="permalink muted"><i class="icon-link"></i></a>{% endif %}</small></h2>{% endif %}
		</hgroup>
		<div><small><i class="icon-time"></i> <time class="pubdate" datetime="{{ _DATE_ | date: "%Y-%m-%d" }}">{{ _DATE_ | date: "%b %d %Y" }}</time>
			| {% for tag in _TAGS_ %}<a href="{{ BASE_PATH }}{{ site.JB.tags_path }}#{{ tag }}" class="label">{{ tag }}</a> {% endfor %}
			{% if _SOURCE_.url  %} | via: {% if _SOURCE_.title %}	<a href="{{ _SOURCE_.url }}" title="{{ _SOURCE_.title }}">{{ _SOURCE_.title }}</a>
			{% else %}	<a href="{{ _SOURCE_.url }}">{{ _SOURCE_.url }}</a>{% endif %}{% endif %}</small>
		</div>
	</header>
	<section class="post">
		{{ _CONTENT_ }}
	</section>
</article>