<article>
	<hgroup>
		<header class="post-header">
			<h2>{{ post.title }} <small>{{ post.description }}</small></h2>
			<h4><small>Published: <time class="pubdate" datetime="{{ post.date | date: "%Y-%m-%d" }}">{{ post.date | date: "%b %d %Y" }}</time> <a href="{{ BASE_PATH }}{{ post.url }}" class="permalink"><i class="icon-link"></i></a>
			<h6>{% for tag in post.tags %} <a href="{{ BASE_PATH }}{{ site.JB.tags_path }}#{{ tag }}-ref" class="label label-info">{{ tag }}</a> {% endfor %}</h6>
			{% if post.via or post.viaURL %}<br> via:
				{% if post.via != empty and post.viaURL != empty %} 	<a href="{{ post.viaURL }}" title="{{ post.via }}">{{ post.via }}</a>
				{% elsif post.via == empty and post.viaURL != empty%}	<a href="{{ post.viaURL }}">{{ post.viaURL }}</a>	{% else %}error{% endif %}{% endif %}
			</small></h4>
		</header>
	</hgroup>
	<section class="post">
		{{ post.content }}
	</section>
</article>